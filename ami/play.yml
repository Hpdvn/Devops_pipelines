---
- hosts: all
  become: yes
  tasks:
    - name: Install Python3
      apt: name=python3 update_cache=yes state=latest

    - name: Install Jupyter
      apt: name=jupyter update_cache=yes state=latest
